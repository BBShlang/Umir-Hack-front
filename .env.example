VITE_API_BASE_URL=http://26.86.179.119:8081

# URL API для публичной верификации по QR (отдельный сервис на порту 8082)
VITE_VERIFY_API_BASE_URL=http://26.86.179.119:8082

# В dev по умолчанию запросы идут напрямую на VITE_API_BASE_URL (обходит Vite proxy, избегает 502/socket hang up с VPN).
# Раскомментируйте, если нужен именно proxy и CORS на бэкенде не разрешает ваш origin:
# VITE_DEV_PROXY=true
